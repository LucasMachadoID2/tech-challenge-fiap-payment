# name: CI
# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main
# jobs:
#   build:
#     runs-on: ubuntu-latest

#     # --- ADICIONE ISSO ---
#     # Cria um container de banco de dados (PostgreSQL como exemplo)
#     services:
#       postgres:
#         image: postgres:14 # Use uma imagem de sua preferência
#         env:
#           POSTGRES_USER: testuser
#           POSTGRES_PASSWORD: testpassword
#           POSTGRES_DB: testdb
#         ports:
#           - 5432:5432
#         # Opção para garantir que o banco esteja pronto antes de continuar
#         options: >-
#           --health-cmd pg_isready
#           --health-interval 10s
#           --health-timeout 5s
#           --health-retries 5
#     # --- FIM DA ADIÇÃO ---

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3
#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: '18'
#       - name: Install dependencies
#         run: npm ci

#       - name: Run migrations
#         run: npx prisma migrate deploy
#         env:
#           DATABASE_URL: "postgresql://testuser:testpassword@localhost:5432/testdb?schema=public"

#       - name: Run tests
#         run: npm test
#         env:
#           DATABASE_URL: "postgresql://testuser:testpassword@localhost:5432/testdb?schema=public"

#       - name: Build application
#         run: npm run dist
#       - name: SonarCloud Scan
#         uses: SonarSource/sonarcloud-github-action@v2
#         with:
#           projectBaseDir: .
#         env:
#           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }} 
#